# 2026-07-05 下一阶段研究任务书

## 目标

根据 `D:\草地遥感\中期\内蒙古草地遥感项目总结_A到O全方向.md` 和前期技术路线评估，下一阶段研究不再继续发散新的 FVC 驱动分析，而是优先补齐开题主线所需的“草原植被亚型分类证据链”。

核心目标：

> 以 ARNC/NDVI 物候阈值法作为轻量化 baseline，建立可独立验证的草原亚型分类产品，并将现有 FVC 趋势、阈值、GeoDetector、SHAP 和政策响应成果接回“亚型变化机制解释”主线。

## 当前证据基础

### 已具备

- 2000-2024 年 FVC、温度、降水长时序数据；
- 260513 产出的 5 类草地亚型图、概率图、置信度图、GMM 状态图和 MK 趋势图；
- 0522/0528 的 FVC 趋势、RESTREND、GeoDetector、RF/SHAP、突变阈值、政策滞后和未来预测结果；
- 包头靶场多源高分辨率分类预实验；
- 徐雪峰论文提供的 ARNC 阈值法、过渡带类别、OA/UA/PA 精度评价和多期类型转移分析范式。

### 主要缺口

- 目前高 R2 结果主要来自 FVC 连续变量预测，不能替代亚型分类精度；
- 缺少本研究区的 ARNC baseline 和过渡带分类结果；
- 缺少独立验证集、混淆矩阵、OA、UA、PA、macro-F1；
- 缺少多期亚型转移矩阵和桑基图；
- 现有驱动分析目标变量多为 FVC/NDVI，而不是亚型转移概率或退化等级。

## 研究路线

### 阶段 1：样本与数据盘点

任务：

1. 盘点 260513 输出的亚型分类、概率、置信度、GMM 状态和趋势产品；
2. 盘点可作为验证的 CSV、GeoJSON、野外样点和 GEE 采样点；
3. 明确训练样本、验证样本和仅作辅助解释的数据边界；
4. 建立 `data_index/classification_sample_inventory.md`。

验收标准：

- 列出每个样本/产品的路径、类别体系、空间范围、年份、分辨率、是否可用于独立验证；
- 明确哪些数据不能直接作为独立验证。

### 阶段 2：ARNC baseline

任务：

1. 基于现有 NDVI/FVC 时序计算年内或跨年 ARNC 指标；
2. 按草甸草原、典型草原、荒漠草原、草原化荒漠/荒漠等类别统计 ARNC 分布；
3. 设计 5 类硬分类和相邻过渡带分类方案；
4. 输出阈值表、分类图、小图和精度表。

验收标准：

- `results/tables/arnc_thresholds.csv`
- `results/tables/arnc_accuracy.csv`
- 混淆矩阵、OA、UA、PA、macro-F1；
- 记录至少一版失败或不理想的阈值实验，避免只保留最好结果。

### 阶段 3：多源机器学习基线

任务：

1. 在同一训练/验证样本上比较 DT、RF、XGBoost 或轻量模型；
2. 输入特征包括 FVC/NDVI 物候、降水、温度、DEM、坡度、土壤或人类活动代理变量；
3. 与 ARNC baseline 比较精度和空间一致性；
4. 输出误分类空间图和特征重要性。

验收标准：

- 同一验证集上的模型对比表；
- 每类 PA/UA/F1；
- 至少一次空间外推验证：内蒙古训练，蒙古国或边界区测试。

### 阶段 4：亚型变化与驱动回接

任务：

1. 基于多期亚型图生成转移矩阵和桑基图；
2. 定义亚型退化等级或转移强度；
3. 以亚型变化强度为目标变量，重跑或改写 GeoDetector/SHAP/阈值分析；
4. 将 FVC 突变阈值作为生态响应解释，而不是分类结果本身。

验收标准：

- 多期亚型转移矩阵；
- 退化方向图：草甸草原 -> 典型草原 -> 荒漠草原 -> 草原化荒漠/荒漠；
- 驱动因子解释结果表；
- 明确哪些结论属于“关联解释”，哪些可以谨慎讨论为“政策响应”。

## 版本记录规则

每次阶段成果提交时，在 `CHANGELOG.md` 记录：

- 输入数据路径和版本；
- 使用脚本；
- 主要输出表格/图件；
- 关键精度指标；
- 当前风险和下一步。

建议提交命名：

- `Add classification sample inventory`
- `Add ARNC baseline prototype`
- `Add ARNC validation tables`
- `Add machine learning baseline comparison`
- `Add subtype transition analysis`

建议标签：

- `v0.2-arnc-baseline`
- `v0.3-classification-validation`
- `v0.4-subtype-transition`
- `v0.5-driver-relink`

## 下一步立即执行

下一步从阶段 1 开始：

1. 扫描 `D:\草地遥感\result\汇报\260513\output` 中的 `.tif`、`.csv` 和关键图件；
2. 扫描 `0522`、`0528` 中可复用的加载脚本和验证结果；
3. 生成 `data_index/classification_sample_inventory.md`；
4. 再决定 ARNC baseline 的输入数据路径和最小可运行脚本。

